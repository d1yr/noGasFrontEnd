import React from 'react';

class Favorite extends React.Component {
    render() {
        //build an array that matches comments to their articles in the article to make this work. will also want to check the length of the array so that we can have a counter here
        return(
        <div></div>
          
          )
    
    }
}

export default Favorite;